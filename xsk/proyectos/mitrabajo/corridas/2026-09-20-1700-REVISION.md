---
fecha: 2026-09-20 17:00
eje: varios (revision)
entorno: codigo
iteracion: 1
version_app: Admin 0.42.03 / Trabajador 0.39.02 / Plataforma 0.29.01
commit: a4c277a
---

Primera pasada de la iteracion 1: **solo revision de codigo y analisis
estatico** (`destructivo: no`). No se corrio nada dinamico contra ningun
entorno. Herramientas: lectura dirigida por el mapa y el modelo de amenazas,
`bandit` (B307 encontro el `eval` de fformulas), `pip-audit` (CVE de
dependencias), y escaneo del historial de git por secretos.

## Resultados

- AUT-01: fallo — abre H-0004 (identidad en cookie sin firmar)
- ENT-04: fallo — abre H-0005 (eval evadible en el motor de formulas; lo detecto bandit B307)
- ENT-01: fallo — abre H-0007 (sin cabeceras de seguridad)
- IDS-01: fallo — abre H-0008 (sin limite de intentos; ausencia confirmada por codigo, falta prueba dinamica)
- IDS-02: fallo — abre H-0006, H-0010, H-0012 (cookies sin Secure; HMAC truncada; sesiones sin revocacion)
- IDS-03: fallo — abre H-0002, H-0011, H-0016 (clave de plataforma con default y sin hash; reset-clave transitorio; cuenta compartida sin trazabilidad)
- IDS-04: fallo — abre H-0017 (alta nivel 1: identidad solo por CUIL)
- IDS-05: fallo — abre H-0009 (PBKDF2 a 100.000 iteraciones)
- DAT-01: fallo — abre H-0001, H-0003 (SESSION_SECRET y PIN con default en el codigo)
- DAT-02: paso — no hay secretos reales en el historial de git; `.env` nunca se commiteo
- DAT-03: fallo — abre H-0013 (traceback sin depurar a stdout; CUIL en el path a Sentry)
- DAT-04: paso — llamadas externas HTTPS; revisar timeouts (ligado a DIS)
- INF-01: fallo — abre H-0014 (dependencias con CVE: python-multipart, starlette, jinja2, python-dotenv; anthropic sin pinnear)
- INF-04: fallo — abre H-0015 (sin CI de tests ni escaneo antes de desplegar)
- DIS-04: fallo — abre H-0018 (endpoint publico con agregados por sindicato y CORS abierto; falta confirmacion dinamica)

## Pendiente (NO corrido en esta pasada)

**No se corrio el modo destructivo ni ningun test dinamico contra un
entorno.** Quedan para la proxima pasada, contra Pruebas:

- AUT-02 (dinamico): explotar el IDOR de H-0004 cambiando la cookie de identidad.
- AUT-03 (dinamico): aislamiento entre sindicatos por id y por `/app/elegir`.
- AUT-04 (dinamico): fail-closed de `PERMISOS_RUTAS` y alcance de seccional/area.
- IDS-01 (dinamico, DESTRUCTIVO): fuerza bruta real contra los cuatro logins.
- DIS-01 (dinamico, DESTRUCTIVO): rate limit y tope de tamano en las rutas caras.
- DIS-02 (dinamico, DESTRUCTIVO): carga con k6, que el 503 degrade sin colgar.
- DIS-03, INF-02, INF-03, INF-05 (configuracion): backups, perimetro, accesos, deploy — algunos dependen de infra que todavia no existe (dominio, Cloudflare, credenciales de S3; ver configuracion.md).
- ENT-02, ENT-03, IA-01, OBS-01, LEY-02 son de la iteracion 2.

## Observaciones

- El `eval` de `validador.py:333` (H-0005) no estaba en las observaciones del
  mapa: lo aporto bandit. Es el hallazgo nuevo mas relevante de esta pasada.
- Bandit ademas confirmo los defaults (hardcoded_password_string) y que los
  `text(f...)` de `dashboard.py`/`db.py` usan parametros o constantes: no
  inyectables (coincide con el mapa). Los B310 (urlopen) son endpoints https
  fijos, sin URL controlada por el usuario.
- La clasificacion de cada hallazgo (probabilidad/dano/complejidad) es la
  PROPUESTA de Code; falta que SDN la confirme (etapa 7).
