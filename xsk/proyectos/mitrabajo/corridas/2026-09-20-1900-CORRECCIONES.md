---
fecha: 2026-09-20 19:00
eje: varios (correcciones)
entorno: codigo
iteracion: 1
version_app: Admin 0.42.03 / Trabajador 0.39.02 / Plataforma 0.30.01
commit: sprint/xsk-5
---

Bloque 4, primer lote de correcciones: las "victorias rápidas" del ranking,
más el estado de resolución (Solucionado / Parcial / Pendiente) que pidió SDN
para el registro. Cada corrección deja su test de regresión en
`test_xsk_correcciones.py`.

## Correcciones

- H-0001 (Crítico 20 → **Solucionado**): `SESSION_SECRET` fail-closed
  (`auth._requerido`), sin default en el código.
- H-0002 (Crítico 20 → **Parcial**): se eliminó el default de la clave de
  plataforma (fail-closed); falta hashear + usuarios nominales (H-0016).
- H-0006 (Alto 12 → **Solucionado** en demo/prod): cookies con Secure +
  SameSite vía `set_cookie_segura`. En pruebas/local queda sin Secure a
  propósito (HTTP + harness de tests); mejora anotada en DESPLIEGUE_RENDER.md.
- H-0008 (Alto 12 → **Parcial**): freno por IP en los cuatro logins (10
  fallos → 60 s); falta el perímetro (INF-05) para que no se saltee rotando
  la IP.

## Resultado

- Bloquean producción: de 7 a **5** (salieron H-0001 y H-0006).
- Estado de resolución: 2 Solucionado, 2 Parcial, 13 Pendiente, 1 Aceptado.
- Suite: `test_xsk_correcciones.py` (7) verde; re-corridos sin romper
  test_entornos, test_xsanders_pagina, test_sesion_renovacion,
  test_empresa_sesion, test_dashboard, test_areas_*, test_tramites,
  test_admin_usuarios, test_marca_plataforma, test_cuil_leer_ajeno.

## Observaciones

- El flag `Secure` no se pudo activar en Pruebas sin romper ~75 tests que
  usan TestClient sobre HTTP; se activó donde están los datos reales
  (demo/prod) y se dejó anotada la mejora (proxy-headers + Secure por
  esquema) en DESPLIEGUE_RENDER.md.
- Pendiente del bloque 4 (por el ranking): H-0004 (identidad firmada, C3),
  H-0003 (usuario+contraseña en la landing, C3), H-0005 (reemplazar eval,
  C3), y el resto de Medios/Bajos. Y sigue pendiente toda la pasada
  dinámica/destructiva contra Pruebas.
