---
fecha: 2026-09-20 22:30
eje: IDS
entorno: codigo
iteracion: 1
version_app: Admin 0.42.04 / Trabajador 0.39.03 / Plataforma 0.31.01
commit: sprint/xsk-r1
---

Sub-sprint R1, etapas 0 y 1 (SPRINT_R1.md).

## Hecho

- **Etapa 0**: modelo `UsuarioPlataforma` + `LogPlataforma`, migración
  `f4a2b7c19d05` que crea las tablas y siembra los dos superadmin
  (snavello, arsantagati) con clave de un solo uso; funciones de acceso.
- **Etapa 1**: `/plataforma/login` acepta usuario nominal (con fallback al
  genérico durante la transición), registra el login en `LogPlataforma`, y
  ante clave transitoria/datos incompletos manda a **`/plataforma/completar`**
  (cambio de clave ≥10 y distinta de la transitoria + carga de CUIL/DNI/mail/
  dirección/teléfono obligatorios, mail único). El panel gatea a los
  pendientes. La sesión lleva el `uid` del usuario (para el rol, etapa 3).

## Verificación

- `test_usuarios_plataforma.py` (13): siembra, lookup, login nominal → completar,
  guardado, validaciones (clave corta / = transitoria / no coinciden / CUIL /
  mail / campos vacíos / mail duplicado), genérico de transición, clave
  vencida, usuario inactivo.
- Sin romper: test_sesion_renovacion, test_plataforma_portada, test_marca_plataforma,
  test_dashboard, test_uso_ia, test_recursos, test_precios_ia, test_migraciones.
- **Navegador**: login snavello/snaSandro → pantalla "Completá tu cuenta" →
  guardar → entró al panel. Datos guardados OK (CUIL normalizado, mail).

## Efecto en el registro

- H-0016 → **Parcial** (usuarios nominales con log; falta sacar el genérico).
- H-0002 → sigue Parcial (ya hay login nominal hasheado; el genérico con
  default sigue por transición).

## Pendiente de R1

- Etapa 2: `/entornos` con el mismo login (el PIN queda como fallback).
- Etapa 3: gestión de usuarios en `/plataforma` (solo superadmin): alta con
  clave transitoria de 7 días, edición, activar/desactivar, resetear clave.
- Etapa 4: cerrar la transición (sacar genérico + PIN) → recién ahí
  H-0002/H-0003/H-0016 pasan a Solucionado.
