---
fecha: 2026-09-20 23:40
eje: INF
entorno: codigo
iteracion: 1
version_app: Admin 0.42.04 / Trabajador 0.39.03 / Plataforma 0.33.01
commit: sprint/xsk-deps
---

Lote INF: dependencias con CVE (H-0014) + CI (H-0015).

## Hecho

- **H-0014 (Parcial)**: `python-multipart` 0.0.17 → 0.0.32 (cierra el DoS de
  uploads y el resto de sus CVE), `jinja2` 3.1.4 → 3.1.6, `python-dotenv`
  1.0.1 → 1.2.3, y `anthropic` pinneado `==0.120.2` (antes rango abierto).
  pip-audit ya no reporta CVE en esas. **Queda starlette**: su fix exige el
  salto mayor fastapi 0.115 → 0.14x + starlette 0.41 → 1.x, un follow-up que
  necesita validar toda la suite; por eso H-0014 queda Parcial.
- **H-0015 (Solucionado)**: `.github/workflows/ci.yml` — en cada push a main
  y cada PR corre **cada `test_*.py` por separado** (regla del proyecto)
  contra Postgres con pgvector, más pip-audit. Un test roto deja de llegar a
  Pruebas en silencio. pip-audit informativo (continue-on-error) hasta cerrar
  el salto de starlette.

## Verificación

- pip-audit sobre requirements: limpio salvo starlette.
- Barrida de 20 archivos con las deps nuevas (uploads, forms, templates,
  sesión): 20/20 verde.

## Pendiente

- Follow-up de H-0014: salto fastapi/starlette a 1.x con la suite completa.
- Sigue bloqueando 1: H-0008 (rate-limit de login), que cierra con el
  perímetro INF-05 (Cloudflare + dominio propio).
