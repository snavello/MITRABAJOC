---
fecha: 2026-09-20 23:20
eje: IDS
entorno: codigo
iteracion: 1
version_app: Admin 0.42.04 / Trabajador 0.39.03 / Plataforma 0.31.02
commit: sprint/xsk-r1-2
---

Sub-sprint R1, etapa 2 (SPRINT_R1.md): la landing /entornos con el mismo login.

## Hecho

- `/entornos/login` (POST usuario+clave): autentica contra el usuario nominal
  de plataforma con `_login_plataforma_nominal` (núcleo compartido con
  `/plataforma/login`), deja la sesión de plataforma —que `_pase_landing` ya
  reconoce— y redirige a /entornos (o a /plataforma/completar si la cuenta
  está pendiente). Registra el login en `LogPlataforma`.
- La puerta (`entornos_pin.html`) ahora ofrece **usuario+clave** arriba y el
  **PIN como fallback** abajo (transición). El PIN se saca en la etapa 4.
- Refactor: el núcleo de login nominal quedó en `_login_plataforma_nominal`,
  usado por las dos puertas (no se duplica la lógica ni la bitácora).

## Verificación

- `test_entornos.py` (11): login nominal entra a la landing, pendiente va a
  completar, credencial mala avisa, y el PIN sigue andando como fallback.
- `test_usuarios_plataforma` (12) y `test_recursos` (10) sin romperse.
- `curl` sin cookies: la puerta muestra los dos formularios.

## Efecto en el registro

- H-0003 → **Parcial** (la landing ya tiene login usuario+clave; el PIN sigue
  como fallback hasta la etapa 4).

## Pendiente de R1

- Etapa 3: gestión de usuarios en /plataforma (solo superadmin): alta con
  clave transitoria de 7 días, edición, activar/desactivar, resetear clave.
- Etapa 4: cerrar la transición (sacar genérico + PIN) →
  H-0002/H-0003/H-0016 a Solucionado.
