---
fecha: 2026-09-20 20:40
eje: varios (dinamica)
entorno: local
iteracion: 1
version_app: Admin 0.42.04 / Trabajador 0.39.03 / Plataforma 0.30.01
commit: sprint/xsk-8
---

Primera pasada DINÁMICA: pedidos HTTP reales contra un uvicorn local
(localhost:8011, base de desarrollo), para confirmar desde afuera las
correcciones y caracterizar lo pendiente. Se corrió local (no Pruebas) por
seguridad: los tests destructivos (fuerza bruta) sobre una base recreable no
tocan ningún entorno compartido. Dos trabajadores de prueba creados y
borrados al final (namespace 209999999xx).

## Resultados

- AUT-02: paso — con la cookie `cuil_trab` forjada al CUIL de otra persona,
  `/api/mis-recibos` (status 200) NO devolvió ni un dato de la víctima: manda
  la sesión firmada. Confirma H-0004 en un round-trip HTTP real.
- AUT-02b: paso — sin sesión y solo con la cookie forjada, status 400, sin
  datos. La identidad no sale de la cookie plana.
- IDS-01: paso — fuerza bruta contra `/trabajador/login`: el freno por IP se
  activó en el intento #11 (destructivo, solo local). Confirma H-0008 a nivel
  app.
- DIS-04: paso (como OBSERVACIÓN) — `/api/entornos/actividad` responde público,
  sin auth, con `Access-Control-Allow-Origin: *` y ~1950 bytes de agregados.
  Confirma que H-0018 sigue ABIERTO (no se corrigió aún).

## Efecto en el registro

- H-0004: corregido → **verificado** (la prueba dinámica AUT-02 confirmó el
  arreglo desde afuera).
- H-0008: sigue **parcial** — el freno a nivel app funciona (intento #11),
  pero la IP sale de X-Forwarded-For (falsificable): el freno robusto es el
  perímetro (INF-05). La prueba lo confirma como mitigación, no como cierre.
- H-0018: sigue **pendiente** — confirmado presente dinámicamente.

## Pendiente de la pasada dinámica

- AUT-03 (aislamiento entre sindicatos por id) y AUT-04 (fail-closed de
  `PERMISOS_RUTAS`, ya cubierto por `test_areas_rutas.py`).
- DIS-01 / DIS-02 (carga real con k6, `MOCK_EXTRACTOR=1`): más pesados,
  quedan para una corrida de carga dedicada.
- Repetir AUT-02/IDS-01 contra **Pruebas** una vez desplegado (hoy Pruebas
  ya sigue `main`, que tiene las correcciones), como confirmación en el
  entorno real.
