---
id: H-0004
titulo: La identidad del trabajador y del empleador viaja en una cookie sin firmar
eje: AUT
test: AUT-01
estado: verificado
probabilidad: 5
dano: 5
complejidad: 3
owasp: A01:2021
asvs: V4.1.1
cwe: CWE-639
aclaracion: Solucionado: la identidad (CUIL/CUIT) ahora va firmada dentro del token de sesion; las 52 lecturas de la cookie plana pasan por _cuil_seguro/_cuit_seguro, que leen de la sesion. Forjar cuil_trab/cuit_emp ya no cambia la identidad.
corregido_en: (rama sprint/xsk-6)
encontrado: 2026-09-20
corrida: 2026-09-20-1700-REVISION
veredicto: CONFIRMED
---

## Que se encontro
`cuil_trab` y `cuit_emp` se emiten como valor plano, sin HMAC (`main.py:5601, 5682, 6050, 6074`), y ningun endpoint las cruza contra el `uid` de la sesion firmada. Ocho rutas se autentican solo con la cookie plana (ver `mapa.md` seccion 1.2); el caso mas claro es `GET /api/mis-recibos` (`main.py:776`), que lee `request.cookies.get("cuil_trab")` y devuelve los recibos de ese CUIL sin mas. Las ~28 rutas que si validan sesion de trabajador tampoco atan `uid` a `cuil_trab`.

## Como se reproduce
Cambiar la cookie `cuil_trab` por el CUIL de otro afiliado y pedir `/api/mis-recibos`, `/api/mis-notificaciones`, `/perfil-foto/{cuil}`, adjuntos de tramite por id. La explotacion dinamica es el test AUT-02, PENDIENTE de correr contra Pruebas.

## Evidencia
`main.py:776-793`, `mapa.md` seccion 1.2. Compromete V1 (recibos), V7 (tramites), V6 (identidad): es la suplantacion entre usuarios que SDN puso como prioridad.

## Que se sugiere
Que la identidad salga siempre de la sesion firmada: guardar el CUIL/CUIT dentro del token HMAC y dejar de leer la cookie plana, o ligar `cuil_trab` al `uid` y verificarlo en cada request. La correccion cambia como se prueban los demas ejes, por eso AUT va primero.

## Historia
- 2026-09-20: abierto en la corrida de revision. Confirmado por codigo; falta la prueba dinamica AUT-02.
- 2026-09-20: corregido. ident en el token firmado (auth.crear_sesion), helpers _cuil_seguro/_cuit_seguro, 52 lecturas reemplazadas, ~32 helpers de test actualizados al modelo seguro. Regresion en test_xsk_correcciones.py (test_h0004_*).
- 2026-09-20: verificado. Pasada dinamica (corrida 2026-09-20-2040-DINAMICA): con cuil_trab forjado, /api/mis-recibos no filtro datos ajenos en un round-trip HTTP real.
