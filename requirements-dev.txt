# Dependencias SOLO de desarrollo/QA -- NO van a requirements.txt: Render
# instala ese archivo en cada deploy y nada de esto se usa en producción.
#
# Instalación (una vez, con el venv activo):
#   pip install -r requirements-dev.txt
#   playwright install chromium
# La suite unitaria (test_*.py, un archivo por proceso) y los robots E2E
# corren con pytest; los test_*.py también se pueden correr como script.
pytest==9.1.1
playwright==1.62.0
pytest-playwright==0.9.0


# XSK (XSANDERS Security Kit): análisis estático de seguridad. Dev/QA, no producción.
bandit==1.9.4
pip-audit==2.10.1
