fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.17
anthropic>=0.69.0
pdf2image==1.17.0
sqlmodel==0.0.22
python-dotenv==1.0.1
psycopg[binary]==3.2.3
alembic==1.19.0
segno==1.6.6
# RAG del convenio. fastembed va PINNEADO EXACTO a propósito: cambió el
# pooling de multilingual-e5-large entre versiones, y eso hace que los
# vectores guardados dejen de ser comparables con las consultas nuevas, en
# silencio. La versión es parte de la identidad del embedding.
fastembed==0.8.0
pgvector==0.4.2
pypdf==6.16.1
pywebpush==2.5.0
# Cliente async para carga/correr_job.py (el generador de carga que corre
# en un Job de Render, disparado desde la pestaña "Tests" de /entornos).
httpx==0.28.1
# Errores no previstos a Sentry, sin datos personales (sentry_config.py). Pinneado exacto: el
# filtrado depende de la forma del evento, y un cambio del SDK puede dejar pasar un campo.
sentry-sdk==2.69.2
